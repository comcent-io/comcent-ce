FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY sbc/go.mod sbc/go.sum ./
RUN go mod download
COPY sbc/ .
RUN CGO_ENABLED=0 go build -o /sbc .

FROM alpine:3.20
RUN apk add --no-cache curl
COPY --from=builder /sbc /usr/local/bin/sbc
ENTRYPOINT ["sbc"]
