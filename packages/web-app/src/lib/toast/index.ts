// Vendored from svelte-french-toast 1.2.0 (MIT, see LICENSE.md), which is
// unmaintained and does not support Svelte 5. Kept to the parts the app uses.
import toast from './core/toast';

export { default as Toaster } from './components/Toaster.svelte';
export { toast };
export default toast;
