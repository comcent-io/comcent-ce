<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  Register with wrong credentials.
  Expected flow: REGISTER -> 401 (challenge) -> REGISTER + wrong digest -> 401.
  SIPp fills [authentication] using -au / -ap; passing a bad password produces a
  digest that the SIP proxy rejects with another 401 (re-challenge) rather than
  admitting the call.  Receiving a second 401 (instead of a 200) is the SIP-edge
  proof that authentication was rejected.
-->
<scenario name="Register Expect Auth Failure">
  <send retrans="500">
    <![CDATA[

      REGISTER sip:[field2]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:[field0]@[field2]>;tag=[call_number]
      To: <sip:[field0]@[field2]>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[field0]@[local_ip]:[field3];transport=udp>
      Expires: 300
      Content-Length: 0

    ]]>
  </send>

  <recv response="401" auth="true" />

  <send retrans="500">
    <![CDATA[

      REGISTER sip:[field2]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:[field0]@[field2]>;tag=[call_number]
      To: <sip:[field0]@[field2]>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[field0]@[local_ip]:[field3];transport=udp>
      Expires: 300
      [authentication]
      Content-Length: 0

    ]]>
  </send>

  <recv response="401" />
</scenario>
